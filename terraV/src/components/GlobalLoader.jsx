const GlobalLoader = () => {
    return (
        <div className="h-screen w-screen flex items-center justify-center">
            <p className="animate-pulse">Initializing TerraVision...</p>
        </div>
    );
};

export default GlobalLoader;